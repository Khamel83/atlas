[Unit]
Description=Atlas Scheduler Timer
Documentation=man:systemd.timer(5)
Requires=atlas-scheduler.service

[Timer]
# Run every 5 minutes
OnCalendar=*:0/5
AccuracySec=1s
Persistent=true

# Run immediately if missed
RandomizedDelaySec=30

[Install]
WantedBy=timers.target